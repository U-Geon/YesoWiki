## 관련 이슈

Closes #012

## 변경 사항 요약

프로젝트의 자동화된 PR 코드 리뷰 에이전트를 `CodeRabbit`에서 `Gemini Code Assist`로 마이그레이션했습니다.

---

### 1. CodeRabbit 환경설정 제거

- 기존 리뷰 규칙과 필터가 작성되어 있던 `.coderabbit.yaml` 파일을 완전히 삭제했습니다.

### 2. Gemini Code Assist 설정 도입

- `.gemini/config.yaml` 생성: 
  - 리뷰 제외 경로(`plans/`, `docs/`, `.agents/`, 마크다운 파일 등)를 설정하여 불필요한 리뷰 코멘트 방지.
  - `comment_severity_threshold` 등 PR 리뷰 기본 정책 설정.
- `.gemini/styleguide.md` 생성:
  - 기존 CodeRabbit 환경설정에 정의되어 있던 프로젝트 특화 리뷰 가이드라인을 `styleguide.md`로 옮겨왔습니다.
  - Next.js 14 App Router 컨벤션(RSC, 데이터 패턴), 보안, Prisma 스키마 검증, 컴포넌트 명명 규칙 등이 포함되어 Gemini 에이전트가 이를 엄격하게 리뷰에 참고할 수 있도록 구성했습니다.

### 3. 프로젝트 문서 최신화

- `plans/012-migrate-gemini-code-assist.md` 플랜 파일 완료 반영
- `.agents/memory/steering.md`의 Learnings 섹션에 해당 아키텍처/인프라 변경 이력 기록 완료

---

## Reviewer 체크리스트

- [x] `.coderabbit.yaml` 파일이 완전히 삭제되었는가
- [x] `.gemini/config.yaml` 설정이 올바르게 반영되었는가
- [x] 기존 리뷰 룰 셋(Next.js App Router, Prisma 규칙 등)이 `.gemini/styleguide.md`로 무사히 이관되었는가
- [x] 관련 문서(`steering.md`, `plans/012`)가 최신화되었는가
